# 2.1.1 — SRS Document (Software Requirements Specification)

**Proyecto:** Memory Service
**Fase:** Analysis (Phase 4)
**Tarea VTT:** MS-018
**Autor:** SA Ejecutor (`0c128e3b-db3b-4e31-b107-0379b5791233`)
**Fecha:** 2026-05-05

---

## 1. Introducción

### 1.1 Propósito

Este documento especifica los requisitos funcionales de Memory Service R1. Define QUÉ debe hacer el sistema — no cómo se implementa. Es el documento maestro que alimenta todas las tareas posteriores de la fase Analysis: NFR (MS-019), Use Cases (MS-020), User Stories (MS-021), Business Rules (MS-022), Acceptance Criteria (MS-024) y Traceability Matrix (MS-025).

### 1.2 Alcance del Sistema

Memory Service R1 es un microservicio independiente (D-MEM-01) que centraliza la memoria de agentes de IA del ecosistema VTT. El sistema expone 11 endpoints REST, opera sobre una base de datos PostgreSQL propia (`memory_service_db`) y almacena archivos JSONL en filesystem. Scope fijo: no se agregan ni quitan endpoints en R1 sin Change Request al PM.

### 1.3 Usuarios y Actores

| Actor | Tipo | Interacción |
|-------|------|-------------|
| **Runtime v1.1** | Sistema | `POST /import` al terminar cada ronda de agente |
| **Prompt Builder v1.3** | Sistema | `GET /context` para ensamblar prompts con historial |
| **Hook Manager VTT** | Sistema | `POST /import-review` para canales VTT_CHANNEL |
| **Equipo humano (PM, TL)** | Humano via UI | Dashboard, Timeline, Cost Report, Upload |
| **DevOps** | Humano/sistema | `GET /health` para monitoreo |

---

## 2. Resolución de Ambigüedades

### AMB-01 — GET /content: contentPreview vs contenido completo

**Resolución:** `GET /conversations/:id/content` lee el archivo JSONL completo desde `/storage/` y lo retorna al cliente. La BD almacena solo `contentPreview` (primeros 500 caracteres del primer turno de asistente) para listados y búsqueda rápida. El endpoint de content NO lee de BD para el cuerpo de la conversación — lee del filesystem (D-MEM-43). Si el archivo no existe en `/storage/`, retorna error `MEM-ERR-404`.

### AMB-02 — Import incremental: ¿actualiza participantes o solo mensajes?

**Resolución:** El import incremental de VTT_CHANNEL (D-MEM-16) es append-only para mensajes (`AgentMessage`). Los participantes (`ConversationParticipant`) se actualizan con upsert: si un nuevo participante aparece, se agrega; los existentes no se modifican. Los campos de la conversación (`title`, `summary`) se actualizan con los valores del último import.

### AMB-03 — activeAgents en dashboard: ventana de tiempo

**Resolución:** `activeAgents` en `GET /dashboard/stats` cuenta agentes distintos (`primaryAgentId`) con al menos una conversación IMPORTED en los últimos 30 días. Ventana fija en R1.

### AMB-04 — Cleanup retry sin archivo en storage

**Resolución:** Si el cleanup job encuentra una conversación en PROCESSING cuyo archivo no existe en `/storage/`, la mueve directamente a ERROR con `errorMessage: "Storage file missing after processing"` sin incrementar retryCount.

### DB-OBS-03 — N+1 en import-review

**Resolución:** El sistema DEBE pre-cargar los catálogos al inicio (startup) y mantenerlos como Maps (`code → id`). Los flujos de import resuelven IDs desde estos Maps, no con queries dentro de loops (DEC-01, SPEC v1.9 §3.3). Documentado como RF-032.

---

## 3. Decisiones SPEC v1.9 Vigentes

| Decisión | Contenido | Impacto en RFs |
|----------|-----------|----------------|
| D-MEM-01 | BD separada (`memory_service_db`) | Sistema standalone |
| D-MEM-05/42 | `@@unique([sourceId, externalSessionId])` | Idempotencia import |
| D-MEM-07/37 | GET /context <500ms fail-fast | SLA contractual |
| D-MEM-08 | Clasificación por reglas determinísticas | Sin ML en R1 |
| D-MEM-13 | Estados: PENDING→PROCESSING→IMPORTED/ERROR | Máquina de estados |
| D-MEM-14 | Endpoints de import separados | JSONL vs VTT_CHANNEL |
| D-MEM-15 | conversationType: TASK_EXECUTION vs REVIEW/CLARIFICATION | Discriminación |
| D-MEM-16 | VTT_CHANNEL import incremental | Append-only |
| D-MEM-18/43 | contentPreview en BD, completo en /storage/ | Storage dual |
| D-MEM-20 | Catálogos extensibles sin migración | INSERT, no deploy |
| D-MEM-25 | Storage filesystem en R1 | Bind mount |
| D-MEM-26 | Solo SERVICE_KEY en R1 | Sin JWT |
| D-MEM-29 | ConversationParticipant multi-agent | N participantes |
| D-MEM-35 | Cleanup job cada 5 min | Recovery automático |
| DB-OBS-01 | `@@unique([conversationId, entityName])` | Sin duplicados en retry |
| DB-OBS-08 | Manejo explícito error P2002 | Race condition |

---

## 4. Estructura de Requisitos

| Feature | Endpoints | RFs |
|---------|-----------|-----|
| **Import** | POST /import, POST /import-review, POST /upload | RF-001 a RF-010 |
| **Context** | GET /context | RF-011 a RF-015 |
| **Content & Search** | GET /content, GET /conversations | RF-016 a RF-020 |
| **Timeline** | GET /agents/:id/timeline | RF-021 a RF-023 |
| **Cost** | GET /projects/:id/cost-report, GET /agents/:id/cost-report | RF-024 a RF-027 |
| **Dashboard & Ops** | GET /dashboard/stats, GET /health, Cleanup Job | RF-028 a RF-033 |

Total: **33 requisitos funcionales** para 11 endpoints + 1 job interno.

---

## Fuentes

- SPEC_MEMORY_SERVICE_v1.9_CONSOLIDADO.md §1, §2, §4, §8, §9, §10, §11
- TL_REVIEW_SPEC_MEMORY_SERVICE_v1.md (AMB-01..04, DEC-01..05)
- DB_REVIEW_SPEC_MEMORY_SERVICE_v1.md (DB-OBS-01..08)
- ADDENDUM_INTEGRACION_MEMORY_SERVICE_v1.1.md §2, §3, §4
- HO_FASE_2_ANALYSIS_MEMORY_SERVICE.md §3
