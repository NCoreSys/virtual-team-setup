# 2.5.2 — Rules List

**Proyecto:** Memory Service
**Fase:** Analysis (Phase 4)
**Tarea VTT:** MS-022
**Autor:** SA Ejecutor (`0c128e3b-db3b-4e31-b107-0379b5791233`)
**Fecha:** 2026-05-05

---

## Import Rules

### BR-001 · Idempotencia de import (Must, Invariant)

El sistema garantiza que importar la misma conversación dos veces no produce duplicados. El constraint `@@unique([sourceId, externalSessionId])` en la tabla Conversation es la fuente de verdad. Si la combinación ya existe con status IMPORTED, el sistema retorna `{ status: "ALREADY_INDEXED", conversationId }` con HTTP 200. En caso de race condition (error Prisma P2002), el sistema captura el error, consulta el registro existente y retorna ALREADY_INDEXED. Esto NO es un error — es comportamiento esperado.

**Ref:** D-MEM-05, D-MEM-42, DB-OBS-08, RF-002

---

### BR-002 · Catch en import delega al cleanup (Must, Invariant)

Si ocurre un error durante el procesamiento de import (escritura en storage, parseo, clasificación), el catch NO mueve la conversación a ERROR directamente. El estado permanece en PROCESSING. El cleanup cron (BR-006) detecta la conversación stuck y la reintenta. ERROR solo ocurre cuando el cleanup agota MAX_RETRIES. Esta regla aplica tanto a `POST /import` como a `POST /import-review`.

**Ref:** AMB-07, TL-02, D-MEM-35, RF-031

---

### BR-003 · Separación BD/Storage (Must, Invariant)

El contenido JSONL completo se almacena en el filesystem (`/storage/{sourceCode}/{conversationId}.jsonl`). La BD almacena solo metadata y `contentPreview` (primeros 500 caracteres del primer turno de asistente). El contenido completo NUNCA se guarda en BD. `GET /content` lee del filesystem, no de BD.

**Ref:** D-MEM-18, D-MEM-43, D-MEM-25, RF-004, RF-016

---

### BR-004 · Storage paths por tipo de conversación (Must, Hard)

| Tipo | Path en storage |
|------|----------------|
| TASK_EXECUTION | `/storage/{sourceCode}/{conversationId}.jsonl` |
| AGENT_REVIEW / AGENT_CLARIFICATION | `/storage/_reviews/{conversationId}.jsonl` |

**Ref:** D-MEM-06, RF-004

---

### BR-005 · Formato externalSessionId para CLAUDE_SDK (Must, Hard)

Las conversaciones importadas por Runtime v1.1 con `sourceCode: CLAUDE_SDK` deben usar el formato `{run_id}:r{N}:{agentRole}` para `externalSessionId`. Donde `run_id` es el UUID de la corrida del Runtime, `N` es el número de ronda (1-based) y `agentRole` es el rol del agente (ej: `be`, `fe`, `tl`).

**Ref:** D-INT-01, D-INT-02, ADDENDUM §2

---

### BR-006 · Cleanup cron cada 5 minutos (Must, Invariant)

El sistema ejecuta un job cron automáticamente cada 5 minutos que busca conversaciones stuck. Criterio de selección: `statusId` en [PENDING, PROCESSING] AND `updatedAt` > NOW() - 10 minutos. El cleanup usa `statusId` directo (no join con `status.code`). Si `retryCount` <= MAX_RETRIES (3): resetea a PENDING e incrementa retryCount. Si `retryCount` > MAX_RETRIES: mueve a ERROR con `errorMessage: "Max retries exceeded"`. Si el archivo no existe en `/storage/`: mueve a ERROR directamente con `errorMessage: "Storage file missing"` sin incrementar retryCount.

**Ref:** D-MEM-35, DB-OBS-02, AMB-04, LIM-10, RF-031

---

### BR-007 · Clasificación determinística sin ML (Must, Hard)

La clasificación post-import usa reglas determinísticas (pattern matching en contenido). Asigna: workType (tabla Classification), topics (tabla ConversationTopic, relación N:M) y entities (tabla ConversationEntity con constraint `@@unique([conversationId, entityName])`). No se usa ML, embeddings ni modelos externos en R1.

**Ref:** D-MEM-08, D-MEM-20, D-MEM-21, DB-OBS-01, RF-005

---

### BR-008 · Import incremental VTT_CHANNEL (Must, Hard)

Si una conversación VTT_CHANNEL ya existe (mismo `[sourceId, externalSessionId]`), el sistema ejecuta import incremental: append-only para mensajes nuevos (AgentMessage), upsert para participantes (ConversationParticipant — agrega nuevos, no modifica existentes), actualización de campos de conversación (`title`, `summary`) con valores del último import. Retorna `{ status: "UPDATED", newMessages: N }`.

**Ref:** D-MEM-16, AMB-BR-02 resuelta, RF-007

---

### BR-009 · Pre-carga de catálogos en startup (Must, Hard)

Al iniciar el servicio, los 10 catálogos se cargan en memoria como Maps (`code → id`). Los flujos de import resuelven IDs desde estos Maps, no con queries individuales. Si un catálogo no puede cargarse, el servicio aborta startup (fail-fast). Si un código de catálogo no existe en el Map durante import, el import falla con error descriptivo.

**Ref:** DEC-01, DB-OBS-03, RF-008, RF-032

---

### BR-010 · ConversationEntity sin duplicados en retry (Must, Invariant)

La tabla ConversationEntity tiene constraint `@@unique([conversationId, entityName])`. En caso de retry (cleanup resetea PROCESSING → PENDING → reimport), el clasificador usa upsert o ignora errores de duplicado al insertar entidades.

**Ref:** DB-OBS-01, RF-033/RNF-004

---

## Context Rules

### BR-011 · GET /context fail-fast <500ms (Must, Hard)

`GET /context` DEBE responder en menos de 500ms (p95). La implementación usa `Promise.all` para 6 queries paralelas envuelto en `Promise.race` con timeout de 500ms. Si el timeout se alcanza, retorna HTTP 504 con `{ error: "MEM-ERR-504" }` inmediatamente. NO hay retry automático, NO hay modo degradado, NO hay cache fallback, NO hay resultados parciales.

**Ref:** D-MEM-07, D-MEM-37, RF-012/RNF-001, AR Review §3.3

---

### BR-012 · Contexto solo conversaciones IMPORTED (Must, Hard)

Todas las queries de contexto filtran exclusivamente conversaciones con status IMPORTED. Conversaciones en PENDING, PROCESSING o ERROR no aparecen en el contexto runtime.

**Ref:** RF-014

---

### BR-013 · projectId siempre obligatorio en contexto (Must, Hard)

`GET /context` requiere `projectId` como query parameter obligatorio. No se puede consultar contexto sin especificar proyecto. `taskId` es opcional y filtra adicionalmente.

**Ref:** D-MEM-39, RF-011

---

## Cost Rules

### BR-014 · Fórmula de costo USD (Must, Hard)

El costo se importa directamente del SDK como `totalCostUsd` del `ResultMessage`. Memory Service no recalcula el costo. Para conversaciones importadas via adapters sin datos de costo (CLAUDE_WEB, CHATGPT, VTT_CHANNEL), `totalCostUsd` es `null`. Estas conversaciones aparecen en el cost-report con costo $0.00 — NO se excluyen del reporte.

Fórmula de referencia (cálculo que hace el SDK, no Memory Service):
```
totalCostUSD = (inputTokens × inputCostPer1k / 1000) + (outputTokens × outputCostPer1k / 1000)
```

**Ref:** RF-026, RF-027

---

### BR-015 · Agentes activos = ventana de 30 días (Must, Hard)

`activeAgents` en `GET /dashboard/stats` cuenta agentes distintos (`COUNT DISTINCT primaryAgentId`) con al menos 1 conversación con status IMPORTED cuyo `startedAt` cae dentro de los últimos 30 días. Ventana fija en R1, no configurable.

**Ref:** AMB-BR-03 resuelta, AMB-03 MS-018, RF-028

---

### BR-016 · Cost-report solo IMPORTED (Must, Hard)

Los endpoints `GET /projects/:id/cost-report` y `GET /agents/:id/cost-report` suman tokens y costos solo de conversaciones con status IMPORTED. Conversaciones en ERROR, PENDING o PROCESSING no cuentan en totales.

**Ref:** RF-027

---

### BR-017 · byWeek requiere SQL raw (Should, Soft)

La agrupación `byWeek` en cost-report usa `DATE_TRUNC('week', "startedAt")` via `prisma.$queryRaw` porque `groupBy` de Prisma no soporta agrupación por semana nativamente. Formato de salida: `YYYY-W##`. Esto es una restricción de implementación, no un cambio de requisito.

**Ref:** TL-08, RF-025

---

## Authorization Rules

### BR-018 · SERVICE_KEY para endpoints de sistema (Must, Hard)

Los endpoints consumidos por sistemas (Runtime, PB, Hook Manager) requieren header `Authorization: Bearer {SERVICE_KEY}`. Si la key es ausente o inválida, retorna HTTP 401. Endpoints: `POST /import`, `POST /import-review`, `GET /context`.

**Ref:** D-MEM-26, RF-010, RF-013

---

### BR-019 · Endpoints públicos en R1 (Must, Hard)

Los endpoints consumidos por UI User y Admin son públicos en R1 (sin autenticación). La decisión de implementar JWT para usuarios finales está diferida a R2. Endpoints: `POST /upload`, `GET /conversations`, `GET /content`, `GET /timeline`, `GET /cost-report` (×2), `GET /dashboard/stats`, `GET /health`.

**Ref:** D-MEM-26, KICKOFF §5.4, ADR-SA-006

---

## Data Rules

### BR-020 · contentPreview = 500 chars del primer turno asistente (Must, Hard)

El campo `contentPreview` en la tabla Conversation almacena los primeros 500 caracteres del contenido del primer turno de rol ASSISTANT. Se calcula durante el import. Es la única porción de contenido que vive en BD. El contenido completo está en `/storage/`.

**Ref:** D-MEM-18, D-MEM-43

---

### BR-021 · primaryAgentId null para multi-agente (Must, Invariant)

Conversaciones de tipo AGENT_REVIEW y AGENT_CLARIFICATION tienen `primaryAgentId = null`. Los participantes se registran en la tabla ConversationParticipant. Solo TASK_EXECUTION tiene `primaryAgentId` no null.

**Ref:** D-MEM-15, D-MEM-29, RF-006

---

### BR-022 · Catálogos extensibles sin migración (Must, Hard)

Nuevos topics, workTypes, fuentes y otros valores de catálogo se agregan con INSERT en BD, no con deploy de código. Los catálogos se recargan en el próximo restart del servicio.

**Ref:** D-MEM-20

---

### BR-023 · Timestamps de contenido, no de import (Must, Hard)

`startedAt` y `endedAt` en Conversation representan el inicio y fin del contenido de la conversación, no del proceso de import. `importedAt` es un campo separado que registra cuándo se completó el import.

**Ref:** D-MEM-31, D-MEM-32

---

## Resumen

| Categoría | BRs | IDs |
|-----------|-----|-----|
| Import | 10 | BR-001..BR-010 |
| Context | 3 | BR-011..BR-013 |
| Cost | 4 | BR-014..BR-017 |
| Authorization | 2 | BR-018..BR-019 |
| Data | 4 | BR-020..BR-023 |
| **Total** | **23** | |

---

## Fuentes

- SPEC_MEMORY_SERVICE_v1.9_CONSOLIDADO.md §2, §7, §9, §10, §11, §12, §13
- TL_REVIEW_SPEC_MEMORY_SERVICE_v1.md (AMB-07, TL-05, TL-08)
- DB_REVIEW_SPEC_MEMORY_SERVICE_v1.md (DB-OBS-01..03)
- ADDENDUM_INTEGRACION_MEMORY_SERVICE_v1.1.md §2
- 2.1.1_srs_document.md §2 (resoluciones previas)
- CIERRE_PM_HANDOFF_PJM_MEMORY_SERVICE_R1.md §4 (limitaciones R1)
